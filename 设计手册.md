🎮 《三消英雄》核心全量开发设计手册 v2.0
1. 核心游戏循环与状态机 (Core Game Loop & State Machine)
游戏运行一个为期 8 个关卡节点的线性流向（Run）。在这个 Run 期间，玩家的常驻资产（卡组、吞噬的属性等）跨关卡永久保留。
1.1 核心状态机流程


[开始游戏 Run] -> 初始化全局常驻变量 & 生成初始【玩家方块堆 Player Deck】
       ↓
[加载当前关卡 STAGES[index]] -> [战斗阶段] -> 玩家在时间限制内疯狂消除 
       ↓
[战斗胜利] -> 进入 [结算与奖励阶段 Reward_Phase]
       ↓
       ├─► 1. 必定触发：从【加成库】进行 “3选1” -> 永久修改【玩家方块堆】或伤害全局变量
       │
       └─► 2. 条件触发：若当前关卡 has_relic_reward == true
              └─► 紧接着触发：从【圣遗物库】进行 “2选1” -> 永久装配至背包
       ↓
[所有奖励选择完毕] -> index++ -> 判定 index 是否超过最大关卡 (8)
       ├─► 是 -> [游戏通关，大数字战绩结算]
       └─► 否 -> 进入下一关


1.2 关卡序列配置数组 (Stage Sequence)
开发 AI 请严格按照以下线性数组结构构建关卡序列：

JSON
STAGES = [
    {"id": 1, "type": "minion", "name": "小怪1", "has_relic_reward": false},
    {"id": 2, "type": "minion", "name": "小怪2", "has_relic_reward": true},
    {"id": 3, "type": "minion", "name": "小怪3", "has_relic_reward": false},
    {"id": 4, "type": "boss",   "name": "boss1", "has_relic_reward": true},
    {"id": 5, "type": "minion", "name": "小怪4", "has_relic_reward": false},
    {"id": 6, "type": "minion", "name": "小怪5", "has_relic_reward": true},
    {"id": 7, "type": "minion", "name": "小怪6", "has_relic_reward": false},
    {"id": 8, "type": "boss",   "name": "boss2", "has_relic_reward": false}
]


2. 常驻方块堆与动态运行池系统 (Player Deck & Active Pool)
本游戏抛弃了传统的“百分比概率掉落”机制，重构为类似《小丑牌（Balatro）》的“不放回抽样卡组轮转”机制。前端无任何数量、概率或卡组余量 UI 提示，对玩家保持纯黑盒状态。
2.1 玩家方块堆 (Player Deck) 的初始化
在整个游戏 Run 开始时，系统根据局外养成决定的当前棋盘边长 $N$（如 5x5 的 $N=5$）生成唯一的真理源：Player Deck。
基础方块：共 4 种颜色（颜色1、颜色2、颜色3、颜色4）。
大盘面卡组池量压缩规则：为保证每局能高频体验卡组轮转，单色基础量不照搬 $N \times N$，而是采用以下数值：
特殊方块数量公式：严格保持为当前卡组内“基础方块总数”的 $1\%$（四舍五入，保底为 1）。
$$\text{Special\_Count} = \max(1, \text{Round}(\text{Total\_Basic\_Count\_In\_Deck} \times 0.01))$$

棋盘尺寸 (N×N)
初始单色配置数量
基础方块总数
特殊方块数量 (1%)
玩家方块堆初始总容量
$5 \times 5$
每色 $15$ 个
$60$ 个
$1$ 个
$61$ 个
$6 \times 6$
每色 $22$ 个
$88$ 个
$1$ 个
$89$ 个
$7 \times 7$
每色 $30$ 个
$120$ 个
$1$ 个
$121$ 个
$8 \times 8$
每色 $39$ 个
$156$ 个
$2$ 个
$158$ 个
$9 \times 9$
每色 $50$ 个
$200$ 个
$2$ 个
$202$ 个
$10 \times 10$
每色 $60$ 个
$240$ 个
$2$ 个
$242$ 个

2.2 动态卡组构筑逻辑 (Dynamic Deck Modifier)
当玩家选择加成（如显示文本：“格子2的概率增加”）时，后端不修改概率常数，而是直接改变 Player Deck 结构：
基础数量追加：根据该颜色在当前卡组中的存量，执行：$\text{追加数量} = \text{Ceil}(\text{当前存量} \times 10\%)$，永久塞入 Player Deck。
特殊方块动态校准：一旦基础方块总数改变，立即重新执行 1% 公式计算，动态对 Player Deck 中的特殊方块进行增删，严格控死 1% 比例。
2.3 抽块与卡组重置 (Refill) 状态机
战斗开始时，将 Player Deck 深拷贝一份倒入当前关卡的即时运行池 Active Pool 中。
重置触发线：当 Active Pool 中的剩余方块总数 $\le 15$ 个 时，在当前消除连锁与伤害结算完毕的第一时间，强制触发 Refill 事件。
Refill 执行逻辑：清空 Active Pool 残余，读取当前最新的、包含所有局内加成演变后的 Player Deck，深拷贝重新填满 Active Pool。
2.4 开局盘面生成防自爆算法 (Safe Opening Board)
加载关卡渲染 $N \times N$ 棋盘时：
直接从 Active Pool 中一次性弹出 $N \times N$ 个方块。
检查这批方块是否包含“直接三连自爆”，以及是否“存在合法走法”。
若检查不通过，严禁将方块放回池子或重新抽块（防止过度消耗卡组）。必须直接在当前取出的这 $N \times N$ 个方块内部重新打乱坐标（Shuffle），直到满足开局条件后再渲染。
3. 时间控制与死局处理系统 (Time & Deadlock Control)
核心战斗具有高压、快节奏特点。初始行动时长为 3秒。
3.1 消除行为对时间的影响
普通连击不暂停：基础消除与连锁消除触发时，倒计时不暂停（参考《宝石迷阵闪电战》）。
时间恢复：每消除一格方块增加 0.1s，多个方块同时消除额外增加 0.1s。
3.2 全局死局洗牌与补时时序 (Global Reshuffle & Time Bonus)
如果在倒计时未结束时，盘面陷入“无步可消（Deadlock）”状态，后端控制器必须保证绝对不允许重新去池子里抽块，且必须严格按照以下时间挂起时序执行：



[局中检测到死局 (Deadlock)]
       ↓
① [Time_System.Pause()] -> 倒计时立刻挂起暂停，防止洗牌占用玩家时间
       ↓
② [Board_System.GlobalShuffle()] -> 仅将盘面上现存的 N*N 个方块坐标打乱，不消耗运行池
       ↓
③ [Time_System.AddSeconds(1.0f)] -> 结算器的总剩余时间永久补偿追加 1.0 秒
       ↓
④ [Time_System.Resume()] -> 恢复时间倒计时，玩家继续操作


4. 战斗与伤害计算系统 (Combat & Damage Formulas)
为了适配 DNF 式的大血条巨龙 Boss（HP 以 X100 层形式表达），伤害公式采用极致的数值膨胀设计。
4.1 伤害核心公式
每次消除产生伤害的基础结构如下：

$$\text{Final\_Damage} = (\text{Base} + \Delta\text{Base}) \times (\text{Mult} + \Delta\text{Mult}) + \text{Flat\_Damage}$$
Base（基础伤害乘区）：由消除的基础方块数量决定。
Mult（倍率乘区）：由 Combo 数、圣遗物等进行叠乘（$\text{XMult}$）或叠加。
Flat_Damage（固定附加伤害）：无条件直接附加的最终伤害。

5. 圣遗物与加成库 (Evolution Build Database)
基于“常驻卡组”和“吞噬数值”的底层架构，设计了以下颠覆传统玩法、破坏规则的流派卡池：
5.1 圣遗物 (Devour & Permanent Growth)
这类圣遗物会修改全局常驻变量 Global_Delta_Base、Global_Delta_Mult 等。
圣遗物A：【炸弹吞噬】
效果描述：玩家合成的所有特殊炸弹（行/列/全清炸弹），在倒计时结束时不会引爆，也不产生任何即时伤害。
吞噬进化：单局结算时，系统收回盘面上所有合成的炸弹。每回收一个行/列炸弹：Global_Delta_Base += 2；每回收一个全清炸弹：Global_Delta_Mult += 0.2。
圣遗物B：【伤害溢出】
效果描述：当场上的怪兽被击杀死亡时，如果玩家该回合打出的最终伤害超过了敌人的剩余血量，触发溢出灵魂吞噬。
计算公式：Global_Flat_Dmg += (本次最终伤害 - 敌人死前剩余HP) * 5%。
圣遗物C：【幸运炸弹】
效果描述：消去任何方块时，该方块的伤害不再是固定的 10 点，而是在 1~30 点之间随机滚动。
计算公式：每次消除时，Base随机1到30
圣遗物D：【倒计时】
效果描述：倒计时3s以内的伤害翻倍，3s以上的消除结算伤害减半
计算公式：当剩余时间＞3s,Mult减半；当剩余时间≤3s，Mult翻倍
5.2 加成库 (Synergetic Buffs)
加成 A：【方块1概率增加】
效果描述：提高方块1的概率，按照前述步骤2.2，将方块1在Player Deck中的数量增加3。
加成 B：【伤害增加】
效果描述：当局Run中，Base+10。
加成 C：【倍率增加】
效果描述：当局Run中，Mult+0.5
加成 D：【固定伤害增加】
效果描述：当局Run中，Flat_Damage+50
加成 D：【时间增加】
效果描述：当局Run中，Time+2s
加成 E：子弹时间】
效果描述：当局Run中，时间恢复额外+0.1s
6. 后端核心代码架构规范 (C# / Unity Implementation Blueprint)
为确保开发 AI 在编写程序时不会由于切换关卡而引发“常驻变量清空”的 Bug，AI 必须严格执行以下底层单例与数据管理架构。



C#
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileType { Color1 = 1, Color2 = 2, Color3 = 3, Color4 = 4, Special = 5 }

/// <summary>
/// 全局常驻运行状态追踪器 (整个 Run 期间单例存在，跨关卡不销毁)
/// </summary>
public class GlobalRunTracker : MonoBehaviour
{
    public static GlobalRunTracker Instance;

    public int currentStageIndex = 0;

    // 唯一真理源：常驻玩家方块堆（类似于卡组构筑游戏中的卡组）
    public List<TileType> playerDeck = new List<TileType>();

    // 吞噬进化系统：永久留存的全局伤害公式修改器
    public int Global_Delta_Base = 0;
    public float Global_Delta_Mult = 0.0f;
    public int Global_Flat_Dmg = 0;

    private void Awake() {
        if (Instance == null) { Instance = this; DontDestroyOnLoad(gameObject); }
        else { Destroy(gameObject); }
    }

    /// <summary>
    /// 响应加成选择：给常驻卡组永久增加某个颜色，并严格校准 1% 的特殊方块比例
    /// </summary>
    public void AddTileBuffToDeck(TileType targetType, float percentage) {
        int currentCount = playerDeck.FindAll(t => t == targetType).Count;
        int amountToAdd = Mathf.CeilToInt(currentCount * percentage); // 向上取整

        for (int i = 0; i < amountToAdd; i++) {
            playerDeck.Add(targetType);
        }

        // 核心公式：强制重新校准特殊方块，恒定保持为基础总量的 1%
        RecalculateAndAdjustSpecialTiles();
    }

    private void RecalculateAndAdjustSpecialTiles() {
        int totalBasicCount = playerDeck.FindAll(t => t != TileType.Special).Count;
        int targetSpecialCount = Mathf.Max(1, Mathf.RoundToInt(totalBasicCount * 0.01f));
        int currentSpecialCount = playerDeck.FindAll(t => t == TileType.Special).Count;
        
        int diff = targetSpecialCount - currentSpecialCount;
        if (diff > 0) {
            for (int i = 0; i < diff; i++) playerDeck.Add(TileType.Special);
        } else if (diff < 0) {
            for (int i = 0; i < Mathf.Abs(diff); i++) playerDeck.Remove(TileType.Special);
        }
    }
}

/// <summary>
/// 局内战斗池管理器 (负责处理不放回抽样与池底重置)
/// </summary>
public class TilePoolManager : MonoBehaviour
{
    public List<TileType> activePool = new List<TileType>();

    public void StartCombat() {
        RefillActivePoolFromDeck();
    }

    /// <summary>
    /// 卡组重置 (Refill)：清空当前残余，深拷贝最新的玩家常驻卡组
    /// </summary>
    public void RefillActivePoolFromDeck() {
        activePool.Clear();
        activePool = new List<TileType>(GlobalRunTracker.Instance.playerDeck);
        Debug.Log("运行池已成功执行 Refill。复制了最新的玩家常驻卡组。");
    }

    /// <summary>
    /// 动态不放回抽块接口 (内置池底 15 个的强制刷新线)
    /// </summary>
    public TileType DrawTileFromPool() {
        // 池底线判断
        if (activePool.Count <= 15) {
            RefillActivePoolFromDeck();
        }

        int randomIndex = Random.Range(0, activePool.Count);
        TileType drawnTile = activePool[randomIndex];
        activePool.RemoveAt(randomIndex); // 不放回
        return drawnTile;
    }
}

/// <summary>
/// 时间与死局控制器
/// </summary>
public class CombatTimeManager : MonoBehaviour
{
    public float remainingTime = 3.0f;
    private bool isPaused = false;

    void Update() {
        if (!isPaused) {
            remainingTime -= Time.deltaTime;
            if (remainingTime <= 0) { TriggerEndRound(); }
        }
    }

    /// <summary>
    /// 严格处理即时死局判定（时序逻辑控制）
    /// </summary>
    public void HandleDeadlockReshuffle() {
        isPaused = true; // ① 挂起暂停倒计时
        
        // ② 执行内部打乱：GridManager.Instance.ExecuteGlobalShuffleNoDraw();
        Debug.Log("触发死局，全盘坐标打乱，未消耗任何卡组池方块。");

        remainingTime += 1.0f; // ③ 给玩家提供额外的 1 秒补偿时间
        
        isPaused = false; // ④ 恢复倒计时，状态回归
    }

    private void TriggerEndRound() { /* 回合结束结算伤害 */ }
}


💡 给开发 AI 的核心提示指令 (可以直接发给 AI)
“请作为专业的 Unity/C# 后端主程，严格阅读并遵循《三消英雄开发手册》。要求完整实现第 2 章的玩家常驻方块堆 (Player Deck) 与 第 3 章的时间死局挂起时序逻辑。确保特殊方块计算、防自爆坐标内部打乱以及死局补时 1 秒的顺序与手册提供的伪代码完全一致。严禁添加任何前端数量可视 UI，保持池子运行机制在后端黑盒处理。”
